// DocuMind AI — API Key Configuration
// ─────────────────────────────────────────────────────────────────────────────
// OPTION A: Pre-set your Anthropic API key here so users don't have to type it.
//
//   1. Replace the empty string below with your key:
//        const DOCUMIND_API_KEY = 'sk-ant-api03-YOUR-KEY-HERE';
//
//   2. Save the file.
//
//   3. ⚠️  IMPORTANT — KEEP THIS FILE PRIVATE:
//      - Add "config.js" to your .gitignore so it is NEVER committed to GitHub.
//      - This file is loaded client-side, so anyone who views source can see the key.
//      - For a public GitHub Pages site, leave this empty and let users enter
//        their own key in the banner — keys are stored in sessionStorage only.
//
// OPTION B: Leave empty and users enter their own key in the on-screen banner.
//   The key is stored in sessionStorage (browser tab only, cleared on close).
//
// ─────────────────────────────────────────────────────────────────────────────

const DOCUMIND_API_KEY = '';   // ← paste your sk-ant-... key here (optional)
